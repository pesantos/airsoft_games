import { DataMomentoPipe } from './data-momento.pipe';

describe('DataMomentoPipe', () => {
  it('create an instance', () => {
    const pipe = new DataMomentoPipe();
    expect(pipe).toBeTruthy();
  });
});
